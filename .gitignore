# OS and editor state
.DS_Store
.Spotlight-V100
.Trashes
Thumbs.db
.vscode/

# Rust/build output
/target/
/build/
/dist/
*.so
*.dylib
*.dll
*.exe

# Local configuration, logs, and generated output
.env
*.log
/.docpact/runs/
/output/
/temp/
/tidas/
/methodfile/
/tidas.zip

# Large local fixtures remain outside Git.
/test_data/*

# Disposable staging and the exclusive import lock for the public-specification
# import. Both are transient tooling state, not executable assets or provenance,
# and neither is committed.
/assets/spec/.staging/
/assets/spec/.import.lock
